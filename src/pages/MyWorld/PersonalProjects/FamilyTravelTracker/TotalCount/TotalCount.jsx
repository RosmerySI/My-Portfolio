import React from 'react';

const TotalCount = ({ total }) => {

    

    return <h2 style={{textAlign:'center',margin:'0'}}>Total Countries Visited:<br/> {total}</h2>;
};

export default TotalCount;
